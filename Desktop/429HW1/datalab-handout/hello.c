#include <stdio.h>

/* count characters in input; 2nd version */ 

	int main() { 
		double nc;

		for (nc = 0; nc < 10 ; ++nc)
			if(nc == 5)
				nc++;
		printf("fuck u");			
		;
	       	printf("%.0f\n", nc);
	
		
}
